# AGENTS.md

Content above and below the managed block is yours — the installer never
touches it. Add repository-specific notes here freely.

<!-- BEGIN softela-ai (managed — edits here are overwritten on update) -->

This repository is `{{PROJECT_ID}}`. Base branch(es): `{{BASE_BRANCHES}}`.
Project config: `{{PROJECT_CONFIG}}`. Installed `{{INSTALLED_AT}}` from the
standards at `{{STANDARDS_PATH}}`.

Read `{{STANDARDS_PATH}}` before writing any code — this file only names the
handful of rules to know before your first tool call, not the full set.

- **Never invent.** No invented endpoint, DTO field, store field, config key
  or prop. If you cannot find something, say so and ask instead of guessing.
- **Reuse before writing.** Search the component's own folder, its parent,
  the shared root folders, and any shared package, before adding a new
  helper, type or hook that might already exist.
- **Follow the documentation standard** for this stack without being asked
  again per prompt — see `{{STANDARDS_PATH}}/code-documentation.md`.
- **Follow the git flow** in `{{STANDARDS_PATH}}/git-flow.md` — rebase to
  stay current, never merge or force-push into a base branch, everything
  reaches `{{BASE_BRANCHES}}` through a pull request.
- **Never write a ticket number, or name a real customer, inside a source
  file.**
- **Scope discipline.** Change what was asked and nothing else; do not mix
  a refactor with a behaviour change in the same PR.

These rules are enforced by hooks, not by memory. A blocked tool call is the
rule working, not a bug — the denial message states the corrected command or
next step; follow it rather than working around it.

If a rule genuinely blocks legitimate work, do not edit this file or any
installed file to route around it. Adjust it locally instead, in
`<agent home>/.softela-ai/overrides.json` (see `docs/internal/CONTRACTS.md` §6
in the infrastructure repository for the format). An override only softens
a rule's severity — it can never turn a denial into a silent pass for
something the rule is right to catch.

<!-- END softela-ai -->
